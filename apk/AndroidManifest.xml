<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.nogrep.torchbutton">
    <!-- versionCode / versionName are injected at build time from module.prop
         (see apk/build.sh) so the APK and the Magisk module never drift. -->

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Torch Button"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="false"
        android:hasCode="true">

        <!-- Settings UI launched from the app drawer. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Torch Button">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile for one-tap enable/disable. -->
        <service
            android:name=".TorchToggleTileService"
            android:label="Torch Button"
            android:icon="@drawable/ic_torch"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Receiver the native daemon broadcasts to (by explicit component, as
             root) to drive the torch. exported=false: no other app can reach it. -->
        <receiver
            android:name=".TorchReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Seeds the real torch state into files/torch_state at boot, including
             before first unlock (DE storage). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
